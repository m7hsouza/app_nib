import 'package:flutter/material.dart';

class AdminPanelSettingsScreen extends StatelessWidget {
  const AdminPanelSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Admin"),
    );
  }
}